#include "login.h"

Login::Login(const std::size_t screen_width, const std::size_t screen_height,
             std::string &user_name) : screen_width(screen_width),
                                                        screen_height(screen_height){

        //render login page


        //wait for user write name and press enter

        // change user name


    }

